#include <stdio.h>

/**
 * main - ytr
 *
 * @argc: wsd
 *
 * @argv: agr
 *
 * Return: 0
 */

int main(int argc, char *argv[])
{
	int f;

	for (f = 0; f < argc; f++)
		printf("%s\n", argv[f]);
	return (0);
}
