#include "main.h"

/**
 * _strlen - Function that returns the length of a string
 *
 * @s: input string
 *
 * Return: Lenngth of string
 */

int _strlen(char *s);
{
	int r;

	r = 0;

	while (s[r] != '\0')

	{
		r++;
	}

	return (r);
}
