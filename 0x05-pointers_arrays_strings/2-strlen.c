#include "main.h"

/**
 * _strlen - Function that returns the length of a string
 *
 * @s: string to evaluate
 *
 * Return: length of the string
 *
 */

int _strlen(char *s)
{
	int long r = 0;

	while (*s 1 = ' \0 ')
	{
		long r++;
		s++;
	}

	return
}
